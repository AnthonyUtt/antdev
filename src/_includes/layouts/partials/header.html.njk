<a class="visually-hidden" href="#main-content">Skip to main content</a>
<header>
  <div>{% include "layouts/partials/brand.svg" %}</div>
  <nav>
    {% for link in site.navigation %}
      <a href="{{ link.url }}" {{ helpers.getLinkActiveState(link.url, page.url) | safe }}>{{ link.text }}</a>
    {% endfor %}
  </nav>
</header>
