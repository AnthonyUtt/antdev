<!-- Cal inline embed code begins -->
<div style="width:100%;height:100%;overflow:scroll" id="my-cal-inline-discovery-session"></div>
<script type="text/javascript">
  (function (C, A, L) { let p = function (a, ar) { a.q.push(ar); }; let d = C.document; C.Cal = C.Cal || function () { let cal = C.Cal; let ar = arguments; if (!cal.loaded) { cal.ns = {}; cal.q = cal.q || []; d.head.appendChild(d.createElement("script")).src = A; cal.loaded = true; } if (ar[0] === L) { const api = function () { p(api, arguments); }; const namespace = ar[1]; api.q = api.q || []; if(typeof namespace === "string"){cal.ns[namespace] = cal.ns[namespace] || api;p(cal.ns[namespace], ar);p(cal, ["initNamespace", namespace]);} else p(cal, ar); return;} p(cal, ar); }; })(window, "https://app.cal.com/embed/embed.js", "init");
  Cal("init", "discovery-session", {origin:"https://app.cal.com"});

  // Get current theme from site settings
  function getCalTheme() {
    if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
      return 'dark';
    }
    return 'light';
  }

  Cal.ns["discovery-session"]("inline", {
    elementOrSelector:"#my-cal-inline-discovery-session",
    config: {"layout":"month_view","useSlotsViewOnSmallScreen":"true","theme":getCalTheme()},
    calLink: "antdev/discovery-session",
  });

  Cal.ns["discovery-session"]("ui", {"cssVarsPerTheme":{"light":{"cal-brand":"#552e84"},"dark":{"cal-brand":"#0061ab"}},"hideEventTypeDetails":false,"layout":"month_view"});

  // Listen for theme changes and update Cal embed
  window.updateCalTheme = function() {
    Cal.ns["discovery-session"]("ui", {"theme": getCalTheme()});
  };
</script>
<!-- Cal inline embed code ends -->
